# Fail fast on peer/engine mismatches instead of silently limping along.
engine-strict=true
# Do not auto-install peer deps; the package declares xterm as an optional peer.
auto-install-peers=false
# Deterministic, reproducible installs in CI.
resolution-mode=highest
